class Solution {
public:
    string sortVowels(string s) {
        string k="";
        unordered_set<char> vowels={'a', 'e', 'i', 'o','u'};
        for(int i=0; i<s.size(); i++) {
            if(tolower(s[i])==vow)
        }
        return s;
    }
};
